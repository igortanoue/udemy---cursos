// variavel de controle ; condição de quando vai parar ; incrementação
// i - index
for (let i = 0;                 i <= 10;                       i += 1) {
    const par = i % 2 === 0 ? 'par' : 'impar';

    console.log(i, par)
}